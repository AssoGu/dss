Init:
On proxy node:
1)mnesia:Create_schema([node()])
2)mnesia:start()
3)database_logic:initDB()
4)proxy_genserver:start_link();

On storage nodes:
1)storage_genserver:start_link();
2)net_adm:ping(proxy_address);


Flow - upload/downloading from/to another storage node,
Pick one storage node
1)storage_logic:upload(FileName) to upload file, for now - make sure file is in ?LocalDBFolder


to do -
add monitoring on added nodes, when they exist we have to update the ring.






